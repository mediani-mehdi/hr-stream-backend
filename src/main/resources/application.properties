spring.application.name=hr-stream

# App Configuration
server.port=8090


# Database Configuration
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/${POSTGRES_DB:${DB_NAME:hr_stream}}}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:${POSTGRES_USER:${DB_USER:postgres}}}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:${POSTGRES_PASSWORD:${DB_PASSWORD:123654}}}
spring.jpa.hibernate.ddl-auto=update
spirng.jpa.show-sql=true

# MinIO (S3 SDK)
#s3.endpoint=${S3_ENDPOINT:http://localhost:9000}
#s3.accessKey=${MINIO_ROOT_USER:minio_admin}
#s3.secretKey=${MINIO_ROOT_PASSWORD:minio_admin_password_123}
#s3.bucketName=${MINIO_BUCKET_NAME:ats-resumes}
#s3.region=${S3_REGION:us-east-1}

# Security Configuration
jwt.secret=${JWT_SECRET:MySecretKey123!@$%^&*()_+{}|:<>?[]\\,./}
jwt.expiration=${JWT_EXPIRATION:3600000}

# Gemini / Google GenAI
# Replace YOUR_API_KEY with your actual Google Gemini API key
gemini.api.key=${GEMINI_API_KEY:YOUR_API_KEY}
# Optional custom API base URL if not using default from Spring AI
# gemini.api.url=${GEMINI_API_URL:https://generativelanguage.googleapis.com}

# Replace YOUR_PROJECT_ID with your actual Google Cloud Project ID
spring.ai.google.genai.project-id=${SPRING_AI_GOOGLE_GENAI_PROJECT_ID:test}
spring.ai.google.genai.api-key=${GEMINI_API_KEY:AIzaSyAw-YeewdPcscM8SE3nro4Q6m9wamd6e_E}
spring.ai.google.genai.chat.options.model=${SPRING_AI_GOOGLE_GENAI_MODEL:gemini-1.5-flash}
